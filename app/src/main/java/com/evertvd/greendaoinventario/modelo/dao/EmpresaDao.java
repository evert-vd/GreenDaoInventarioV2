package com.evertvd.greendaoinventario.modelo.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.evertvd.greendaoinventario.modelo.Empresa;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "EMPRESA".
*/
public class EmpresaDao extends AbstractDao<Empresa, Long> {

    public static final String TABLENAME = "EMPRESA";

    /**
     * Properties of entity Empresa.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Codempresa = new Property(1, int.class, "codempresa", false, "CODEMPRESA");
        public final static Property Empresa = new Property(2, String.class, "empresa", false, "EMPRESA");
        public final static Property Estado = new Property(3, int.class, "estado", false, "ESTADO");
    }

    private DaoSession daoSession;


    public EmpresaDao(DaoConfig config) {
        super(config);
    }
    
    public EmpresaDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"EMPRESA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"CODEMPRESA\" INTEGER NOT NULL UNIQUE ," + // 1: codempresa
                "\"EMPRESA\" TEXT NOT NULL UNIQUE ," + // 2: empresa
                "\"ESTADO\" INTEGER NOT NULL );"); // 3: estado
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"EMPRESA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Empresa entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getCodempresa());
        stmt.bindString(3, entity.getEmpresa());
        stmt.bindLong(4, entity.getEstado());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Empresa entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getCodempresa());
        stmt.bindString(3, entity.getEmpresa());
        stmt.bindLong(4, entity.getEstado());
    }

    @Override
    protected final void attachEntity(Empresa entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Empresa readEntity(Cursor cursor, int offset) {
        Empresa entity = new Empresa( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // codempresa
            cursor.getString(offset + 2), // empresa
            cursor.getInt(offset + 3) // estado
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Empresa entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCodempresa(cursor.getInt(offset + 1));
        entity.setEmpresa(cursor.getString(offset + 2));
        entity.setEstado(cursor.getInt(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Empresa entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Empresa entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Empresa entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
